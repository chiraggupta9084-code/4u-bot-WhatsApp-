services:
  - type: web
    name: 4u-whatsapp-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: WHATSAPP_TOKEN
        sync: false
